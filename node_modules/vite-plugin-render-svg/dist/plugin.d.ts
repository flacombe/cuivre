import { Plugin } from 'vite';
export interface RenderSVGOptions {
    pattern: string;
    urlPrefix: string;
    scales?: number[];
    outputOriginal?: boolean;
}
export interface Manifest {
    [scale: string]: {
        [name: string]: string;
    };
}
declare module 'virtual:render-svg' {
    const manifest: Manifest;
}
export declare function renderSVG({ pattern, urlPrefix, scales, outputOriginal: copyOriginal }: RenderSVGOptions): Plugin[];
